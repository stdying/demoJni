#include <classlib/thread.h>#include "JBoardAdapter.h"class LevelThread; // forward declarationclass CBoard {    private:        int length;        int level;        JBoardAdapter* jBoard;        LevelThread*   levelThread;    public:        CBoard();        void setJBoard(JBoardAdapter* jBoard);        void setLength(int len);        int  getLength();        void levelChanged(int level);};class LevelThread : public TThread {    private:        CBoard* cBoard;        int Run();    public:        LevelThread(CBoard*);};